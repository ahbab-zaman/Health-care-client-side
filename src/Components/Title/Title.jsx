const Title = ({title}) => {
    return (
        <div className="py-6">
            <h1 className="text-3xl font-bold text-[#333333] text-center uppercase">{title}</h1>
        </div>
    );
};

export default Title;